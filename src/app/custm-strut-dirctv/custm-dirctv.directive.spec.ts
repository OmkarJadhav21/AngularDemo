import { CustmDirctvDirective } from './custm-dirctv.directive';

describe('CustmDirctvDirective', () => {
  it('should create an instance', () => {
    const directive = new CustmDirctvDirective();
    expect(directive).toBeTruthy();
  });
});
